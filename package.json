{
  "name": "finanzas",
  "version": "2.4.0",
  "private": true,
  "description": "Gestion personal de nomina, distribucion de gastos y huchas de ahorro.",
  "scripts": {
    "build": "node scripts/copy-www.mjs",
    "sync": "npm run build && cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
